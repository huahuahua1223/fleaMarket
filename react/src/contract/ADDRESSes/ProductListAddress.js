const contractAddrArray = require('../address.json');
const productListAddress = contractAddrArray.ProductListAddr;

const ProductListAddress = productListAddress;
export default ProductListAddress;